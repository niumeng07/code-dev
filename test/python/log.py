import logging

FORMAT = '%(filename)s, %(lineno)s, %(asctime)s, %(message)s'
logging.basicConfig(format=FORMAT)
logger = logging.getLogger()
logger.warning('connection reset', extra={})
logger.warning('connection reset', extra={})
logger.warning('connection reset', extra={})
logger.warning('connection reset', extra={})
