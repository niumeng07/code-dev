#include <math.h>

#include <algorithm>
#include <iostream>

using namespace std;

int main(int argc, char *argv[]) {
  cout << std::max(static_cast<size_t>(1), static_cast<size_t>(0)) << endl;
  return 0;
}
