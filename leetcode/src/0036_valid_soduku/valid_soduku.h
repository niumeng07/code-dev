#include <vector>
using namespace std;
class ValidSoduku {
public:
    bool isValidSudoku(vector<vector<char>>& board) {
        for(int  i = 0; i < board.size(); i++)
        {
            //vector<char> row = board[i];//当前行
            for(int j = 0; j < board.size(); j++)
            {
            }
        }
        return true;
    }
};
