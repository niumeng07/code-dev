#include <vector>
#include <string>
#include <iostream>
using namespace std;

/* dfs算法:每个位置可以放符号或不放符号
 * */
class Solution {
public:
    vector<string> addOperators(string num, int target) {
        vector<string> result;
        return result;
    }
};
int main(){
    string ori = "123";
    int target = 6;
    Solution su;
    vector<string> result = su.addOperators(ori, target);
}
