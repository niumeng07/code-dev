#include <iostream>
#include <vector>
#include <string>
#include "Solution.h"

using namespace std;

int main()
{
    Sulution.su;
    vector<string> strs({"eat", "tea", "tan", "ate", "nat", "bat"});
    vector<vector<string> > res = su.groupAnagrams(strs);
    return 0;
}
