#include <iostream>
#include <vector>
using namespace std;
class Solution {
public:
    /*
     * 思路: 见readme
     * */
    string convert(string s, int numRows) {
        vector<string> tmp;
        bool u = true;
        int start = 0;
        // 定义end
        while(true)
        {}
        return "";
    }
};
