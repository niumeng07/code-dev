#include <string>
#include <iostream>
using namespace std;

class Solution_Review{
public:
    // 从中间往两边延伸，并记下当前最长串的左右顶点
    // 处理中间两个字符相同的情况
    // bcb
    // bccb
    string findLongPalind(string input)
    {
        int left = 0, right = 0;
        int maxlen = 1;
        int i = 0, j = 0;
    }
};
