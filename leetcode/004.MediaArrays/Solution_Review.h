#include <iostream>
#include <vector>
using namespace std;

class Solution_Review{
public:
    float findM(vector<int>& left, vector<int>& right){
        int ls = 0, rs = 0, le = left.size() - 1, re = right.size() - 1;
    }
};
